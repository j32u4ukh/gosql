syntax = "proto3";

option go_package = ".;pbgo";

// 編譯：執行 compile_protobuf.bat
message Player {    
    // {"comment": "玩家 ID", "size": 20, "default": "AI", "primary_key": "default"}
    int64 id = 1;
}
